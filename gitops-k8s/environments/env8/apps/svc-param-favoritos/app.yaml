apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: svc-param-favoritos-env8
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/SEU_GITHUB/gitops-k8s.git
    targetRevision: HEAD
    path: environments/env8/apps/svc-param-favoritos/manifest
  destination:
    server: https://kubernetes.default.svc
    namespace: env8
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
